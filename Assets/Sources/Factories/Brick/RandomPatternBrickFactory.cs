using Sources.BricksLogic;
using UnityEngine;

namespace Sources.Factories
{
    public sealed class RandomPatternBrickFactory// : IBrickFactory
    {
        //private readonly Vector3Int _position;
        //private readonly BrickPattern[] _patterns;

        //public RandomPatternBrickFactory(Vector3Int position, BrickPattern[] patterns)
        //{
        //    _position = position;
        //    _patterns = patterns;
        //}

        //private BrickPattern RandomPattern => _patterns[Random.Range(0, _patterns.Length)];

        //public IBrick Create()
        //{
        //    return new Brick(_position, RandomPattern);
        //}
    }
}